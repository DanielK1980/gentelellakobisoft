<div class="col-md-3 left_col menu_fixed">
    <div class="left_col scroll-view">
        <div class="navbar nav_title" style="border: 0;">
            <a href="#" class="site_title"> <span  style="margin-left: 20px">Kobisoft.pl</span></a>
        </div>

        <div class="clearfix"></div>

        <!-- menu profile quick info -->
        <div class="profile">
          <!--  <div class="profile_pic">
                <img src="{{ asset('assets/images/img.jpg') }}" alt="..." class="img-circle profile_img">
            </div>
          -->
            <div class="profile_info" style="margin-left: 20px">
                <span>Witaj,</span>
                <h2>{{ app.user.username }}</h2>
            </div>
        </div>
        <!-- /menu profile quick info -->

        <div class="clearfix"></div>

        <!-- sidebar menu -->
        <div id="sidebar-menu" class="main_menu_side hidden-print main_menu">
            <div class="menu_section">
                <h3>Klienci</h3>
            {{ knp_menu_render('InfogoldAccountBundle:Builder:Clients', {'allow_safe_labels': true}) | raw }}
            </div>
            <div class="menu_section">
                <h3>Pracownicy</h3>
            {{ knp_menu_render('InfogoldAccountBundle:Builder:Consultants', {'allow_safe_labels': true}) | raw }}
            </div>
            <div class="menu_section">
                <h3>Produkty</h3>
            {{ knp_menu_render('InfogoldAccountBundle:Builder:Products', {'allow_safe_labels': true}) | raw }}
            </div>
            <div class="menu_section">
                <h3>Ustawienia</h3>
            {{ knp_menu_render('InfogoldAccountBundle:Builder:Settings', {'allow_safe_labels': true}) | raw }}
            </div>
            
        </div>
        <!-- /sidebar menu -->

        <!-- /menu footer buttons -->
        <div class="sidebar-footer hidden-small">
            <a href="{{ path('fos_user_profile_edit')}}" data-toggle="tooltip" data-placement="top" title="Ustawienia">
                <span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
            </a>

            <a data-toggle="tooltip" data-placement="top" title="Logout" href="{{ path('fos_user_security_logout') }}">
                <span class="glyphicon glyphicon-off" aria-hidden="true"></span>
            </a>
        </div>
        <!-- /menu footer buttons -->
    </div>
</div>