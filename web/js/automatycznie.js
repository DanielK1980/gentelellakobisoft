/*
  $(function() {
      //  $(document).ready(function () {
    var adres = $('td#adres').text();
    var telefon = $('td#telefon').text();
    var email = $('td#email').text();
    $("select#infogold_kliencibundle_kontaktytype_RodzajKontaktu").on('change', function() {
       
        if ($(this).val() === '1') {
            $('#infogold_kliencibundle_kontaktytype_DaneKontaktowe').val(telefon);
        }
        else if ($(this).val() === '2') {
            $('#infogold_kliencibundle_kontaktytype_DaneKontaktowe').val(email);
        }
        else if ($(this).val() === '3') {
            $('#infogold_kliencibundle_kontaktytype_DaneKontaktowe').val(adres);
        }
        else
            $('#infogold_kliencibundle_kontaktytype_DaneKontaktowe').val('');
    });
  // });
   
    });
    */
  //infogold_kliencibundle_kontaktytype_DaneKontaktowe
  /*
  $(document).ready(function () {
      
      window.onclose = alert("fuck");
   window.addEventListener("beforeunload", function (e) {

        $.ajax({
            url: "/Symfony2_8_new/web/app_dev.php/konsultant/browser",
            type: "POST",
            async: false,
            data: "action=checklogin"

        });                           //Webkit, Safari, Chrome
    });
    
    });
    */